---
phase: 04-execution
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/wizard.rs
  - src/state.rs
autonomous: true

must_haves:
  truths:
    - "User sees real-time progress during rebuild"
    - "User understands what phase the rebuild is in"
    - "Errors are displayed with helpful context"
  artifacts:
    - path: "src/wizard.rs"
      provides: "ExecutionStep with progress display"
      exports: ["ExecutionStep", "ExecutionStatus"]
---

<objective>
Add TUI integration for execution progress display - real-time logs, phase indicators, and error handling.

Purpose: User visibility into what's happening during system changes
Output: ExecutionStep in wizard with streaming progress display
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-execution/04-03-SUMMARY.md

# Depends on 04-03
- NixOSExecutor.rebuild() with streaming output
- RebuildStatus enum (Pending, Downloading, Building, Activating, Success, Failed)
- OutputLine struct for individual output lines

# Execution flow
1. Git commit (04-02) - quick
2. nixos-rebuild (04-03) - can take 5-15 minutes
3. Success/error display
4. Rollback option on failure
</context>

<tasks>

<task type="auto">
  <name>Add Step::Execution to Step enum</name>
  <files>src/state.rs</files>
  <action>
    Update Step enum in src/state.rs:
    
    #[derive(Clone, Copy, Debug, PartialEq, Eq)]
    pub enum Step {
        Welcome,
        Timezone,
        Keyboard,
        Account,
        Paths,
        Summary,
        Generate,
        Execution,  // NEW: Run git commit and nixos-rebuild
    }
    
    Update Step::all_steps() to include Execution
    Update Step::title() to return "Execution" for Execution variant
  </action>
  <verify>grep -r "Step::Execution" src/state.rs && cargo check 2>&1 | grep -c "^error"</verify>
  <done>Step enum includes Execution step</done>
</task>

<task type="auto">
  <name>Create ExecutionStep with status enum</name>
  <files>src/wizard.rs</files>
  <action>
    Add to src/wizard.rs:
    
    #[derive(Clone, Debug, PartialEq)]
    pub enum ExecutionStatus {
        Idle,
        GitCommitting,
        GitComplete,
        Rebuilding { phase: String, progress: Option<String> },
        Success,
        Failed { error: String, can_rollback: bool },
    }
    
    pub struct ExecutionStep {
        pub status: ExecutionStatus,
        pub output_lines: Vec<String>,
        pub error_lines: Vec<String>,
    }
    
    impl ExecutionStep {
        pub fn new() -> Self {
            Self {
                status: ExecutionStatus::Idle,
                output_lines: Vec::new(),
                error_lines: Vec::new(),
            }
        }
    }
    
    impl WizardStep for ExecutionStep {
        fn title(&self) -> &'static str {
            "Apply Configuration"
        }
        
        fn render(&self, frame: &mut Frame, state: &WizardState, area: Rect) {
            // Render based on status:
            // - Idle: Show "Press Enter to begin"
            // - GitCommitting: Show commit output
            // - GitComplete: Show "Git commit successful"
            // - Rebuilding: Show phase, progress, and recent output lines
            // - Success: Show "Configuration applied successfully!"
            // - Failed: Show error with rollback option
        }
        
        fn handle_input(&mut self, event: Event, state: &mut WizardState) -> Result<(), String> {
            // Enter: Start execution (git commit + nixos-rebuild)
            // Esc: Go back (only if Idle)
        }
        
        fn is_complete(&self, state: &WizardState) -> bool {
            self.status == ExecutionStatus::Success
        }
    }
  </action>
  <verify>cargo check 2>&1 | grep -c "^error"</verify>
  <done>ExecutionStep exists with status enum and render logic</done>
</task>

<task type="auto">
  <name>Implement execution workflow</name>
  <files>src/wizard.rs</files>
  <action>
    Implement ExecutionStep.handle_input() to run the full workflow:
    
    1. Git commit (using GitExecutor from 04-02)
       - stage_all() -> commit_configs()
       - Stream output to output_lines
       - Update status to GitCommitting, then GitComplete
    
    2. nixos-rebuild (using NixOSExecutor from 04-03)
       - Call rebuild_with_callback()
       - For each OutputLine, add to output_lines and update status
       - On RebuildStatus::Success -> ExecutionStatus::Success
       - On RebuildStatus::Failed -> ExecutionStatus::Failed with error
    
    3. Error handling
       - Capture stderr lines in error_lines
       - Provide helpful error message based on common failures
       - Set can_rollback based on whether previous generation exists
    </action>
  <verify>cargo check 2>&1 | grep -c "^error"</verify>
  <done>Full execution workflow runs and updates status appropriately</done>
</task>

<task type="auto">
  <name>Add create_current_step for Execution</name>
  <files>src/wizard.rs</files>
  <action>
    Update create_current_step() in src/wizard.rs:
    
    pub fn create_current_step(step: Step) -> Box<dyn WizardStep> {
        match step {
            // ... existing cases ...
            Step::Execution => Box::new(ExecutionStep::new()),
        }
    }
  </action>
  <verify>grep -r "Step::Execution" src/wizard.rs && cargo check 2>&1 | grep -c "^error"</verify>
  <done>Execution step is created by create_current_step()</done>
</task>

</tasks>

<verification>
- [ ] cargo check passes with 0 errors
- [ ] ExecutionStep renders correctly in each status
- [ ] User can start execution with Enter
- [ ] Progress updates in real-time
- [ ] Errors show helpful context
</verification>

<success_criteria>
- User sees what phase rebuild is in (Downloading/Building/Activating)
- Output scrolls in TUI as it happens
- Errors are not just raw nix output - they're explained
- Success message confirms configuration applied
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution/04-04-SUMMARY.md`
</output>
