---
phase: 04-execution
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - src/wizard.rs
  - src/executor/nixos.rs
autonomous: true

must_haves:
  truths:
    - "User can rollback to previous configuration on failure"
    - "User receives completion confirmation"
    - "User knows what to do next after successful install"
  artifacts:
    - path: "src/wizard.rs"
      provides: "CompletionStep and rollback functionality"
---

<objective>
Implement rollback capability and completion screen with next steps.

Purpose: Safety net for failures and clear completion experience
Output: CompletionStep and rollback workflow
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-execution/04-04-SUMMARY.md

# Depends on 04-04
- ExecutionStep with Failed status containing error and can_rollback flag
- GitExecutor with commit history

# Rollback strategy
1. Before rebuild, capture previous generation: `nixos-rebuild list-generations`
2. On failure, offer: "Rollback to previous generation" or "Stay on current (possibly broken)"
3. Rollback command: `sudo nixos-rebuild switch --rollback`

# Completion flow
1. Show success message
2. Show what was configured (hostname, desktop environment, etc.)
3. Show next steps:
   - "Log out and back in to use Niri"
   - "Your Noctalia shell is configured"
   - "Git commit created for your configs"
</context>

<tasks>

<task type="auto">
  <name>Add rollback capability to NixOSExecutor</name>
  <files>src/executor/nixos.rs</files>
  <action>
    Add to NixOSExecutor:
    
    /// Get list of generations before rebuild
    pub fn capture_generations(&self) -> Result<Vec<Generation>, ExecutorError> {
        // Run nixos-rebuild list-generations
        // Parse output to extract generation numbers and dates
    }
    
    /// Rollback to previous generation
    pub fn rollback(&self) -> Result<RebuildStatus, ExecutorError> {
        // Run: sudo nixos-rebuild switch --rollback
        // Stream output and return status
    }
    
    #[derive(Debug, Clone)]
    pub struct Generation {
        pub number: i32,
        pub date: String,
        pub current: bool,
    }
  </action>
  <verify>cargo check 2>&1 | grep -c "^error"</verify>
  <done>Rollback function exists in NixOSExecutor</done>
</task>

<task type="auto">
  <name>Update ExecutionStep for rollback handling</name>
  <files>src/wizard.rs</files>
  <action>
    Update ExecutionStatus enum:
    
    #[derive(Clone, Debug, PartialEq)]
    pub enum ExecutionStatus {
        // ... existing variants ...
        Failed {
            error: String,
            can_rollback: bool,
            previous_generation: Option<i32>,
        },
    }
    
    Update ExecutionStep.handle_input() for failed state:
    - On Enter when Failed: Call rollback if can_rollback
    - On 'r' key: Explicit rollback option
    - On 'c' key: Continue anyway (stay on current)
  </action>
  <verify>cargo check 2>&1 | grep -c "^error"</verify>
  <done>ExecutionStep handles rollback input</done>
</task>

<task type="auto">
  <name>Create CompletionStep</name>
  <files>src/wizard.rs</files>
  <action>
    Add to src/wizard.rs:
    
    pub struct CompletionStep;
    
    impl WizardStep for CompletionStep {
        fn title(&self) -> &'static str {
            "Complete"
        }
        
        fn render(&self, frame: &mut Frame, state: &WizardState, area: Rect) {
            let hostname = state.hostname.as_deref().unwrap_or("unknown");
            let username = state.username.as_deref().unwrap_or("unknown");
            
            let text = vec![
                "✅ Installation Complete!".to_string(),
                "".to_string(),
                format!("Your NixOS system '{}' has been configured.", hostname),
                format!("User '{}' has been set up with Niri and Noctalia.", username),
                "".to_string(),
                "Next Steps:".to_string(),
                "1. Log out and log back in (or restart)".to_string(),
                "2. Select 'Niri' from the display manager".to_string(),
                "3. Your Noctalia shell will be ready!".to_string(),
                "".to_string(),
                "Configuration is tracked in git at:".to_string(),
                "  ./milos-output".to_string(),
                "".to_string(),
                "Press Enter to exit the installer.".to_string(),
            ];
            
            let paragraph = Paragraph::new(text.join("\n"))
                .style(Style::default().fg(Color::Green))
                .block(Block::new().title("Installation Complete"));
            
            frame.render_widget(paragraph, area);
        }
        
        fn handle_input(&mut self, event: Event, state: &mut WizardState) -> Result<(), String> {
            if let Event::Key(key) = event {
                if key.code == crossterm::event::KeyCode::Enter {
                    state.mark_step_complete();
                    // Wizard loop will exit since all steps complete
                }
            }
            Ok(())
        }
        
        fn validate(&self, _state: &WizardState) -> Result<(), String> {
            Ok(())
        }
        
        fn is_complete(&self, state: &WizardState) -> bool {
            state.is_current_step_complete()
        }
    }
    
    Update Step enum to include Completion
    Update create_current_step() to return CompletionStep
  </action>
  <verify>cargo check 2>&1 | grep -c "^error"</verify>
  <done>CompletionStep exists and is accessible</done>
</task>

<task type="auto">
  <name>Update state machine flow</name>
  <files>src/state.rs</files>
  <action>
    Ensure wizard flow is correct:
    Summary -> Generate -> Execution -> Completion
    
    Update WizardState::is_complete() if needed
    Ensure step order in Step enum matches expected flow
  </action>
  <verify>cargo check 2>&1 | grep -c "^error"</verify>
  <done>Wizard flow is correct: Summary -> Generate -> Execution -> Completion</done>
</task>

</tasks>

<verification>
- [ ] cargo check passes with 0 errors
- [ ] Rollback command runs when user selects it
- [ ] CompletionStep displays correct information
- [ ] User can exit installer after completion
- [ ] All steps marked complete at the end
</verification>

<success_criteria>
- Rollback works when previous generation exists
- Completion screen shows hostname, user, and next steps
- Installer exits cleanly on Enter
- Phase 4 success criteria all met
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution/04-05-SUMMARY.md`
</output>
