---
phase: 04-execution
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/executor/git.rs
  - src/executor/mod.rs
autonomous: true

must_haves:
  truths:
    - "Generated configs are committed to git"
    - "Git commit message includes user and timestamp"
  artifacts:
    - path: "src/executor/git.rs"
      provides: "GitExecutor struct for config commits"
      exports: ["GitExecutor", "commit_configs"]
---

<objective>
Implement git initialization and commit workflow for generated configurations.

Purpose: Ensures config changes are tracked and can be rolled back
Output: GitExecutor with commit_configs() function
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-execution/04-01-SUMMARY.md

# Depends on 04-01
- run_command() function for executing git commands
- CommandOutput struct for capturing results
- ExecutorError for error handling

# Git workflow
1. git init (if not already a repo)
2. git add .
3. git commit -m "milos: Generate <hostname> config for <username>"
</context>

<tasks>

<task type="auto">
  <name>Create GitExecutor struct</name>
  <files>src/executor/git.rs, src/executor/mod.rs</files>
  <action>
    Create src/executor/git.rs:
    
    #[derive(Debug)]
    pub struct GitExecutor {
        repo_path: PathBuf,
    }
    
    impl GitExecutor {
        /// Create new GitExecutor for the given output directory
        pub fn new(repo_path: PathBuf) -> Self {
            Self { repo_path }
        }
        
        /// Initialize git repo if needed
        pub fn init_or_verify(&self) -> Result<(), ExecutorError> {
            // Check if .git exists
            // If not, run git init
            // If yes, verify it's a valid repo
        }
        
        /// Stage all generated files
        pub fn stage_all(&self) -> Result<(), ExecutorError> {
            run_command(&["git", "add", "."], Some(&self.repo_path))?;
            Ok(())
        }
        
        /// Create commit with generated configs
        pub fn commit_configs(
            &self,
            hostname: &str,
            username: &str,
        ) -> Result<(), ExecutorError> {
            let message = format!(
                "milos: Generate {} config for {}\n\nGenerated by milos_niri installer",
                hostname, username
            );
            run_command(&["git", "commit", "-m", &message], Some(&self.repo_path))?;
            Ok(())
        }
        
        /// Get the current commit hash (for rollback reference)
        pub fn get_current_commit(&self) -> Result<Option<String>, ExecutorError> {
            // Run git rev-parse HEAD and capture output
        }
    }
    
    Update src/executor/mod.rs to include: pub mod git;
  </action>
  <verify>cargo check 2>&1 | grep -c "^error"</verify>
  <done>GitExecutor struct exists with init, stage, and commit methods</done>
</task>

<task type="auto">
  <name>Implement git status check</name>
  <files>src/executor/git.rs</files>
  <action>
    Add to GitExecutor:
    
    /// Check if directory is a git repository
    pub fn is_repo(&self) -> bool {
        self.repo_path.join(".git").exists() && self.repo_path.join(".git").is_dir()
    }
    
    /// Get list of staged/unstaged files
    pub fn get_status(&self) -> Result<Vec<String>, ExecutorError> {
        // Run git status --porcelain and parse output
    }
    
    /// Verify git is installed
    pub fn git_available() -> bool {
        // Try to run git --version
    }
  </action>
  <verify>cargo check 2>&1 | grep -c "^error"</verify>
  <done>Git status and availability checks work</done>
</task>

</tasks>

<verification>
- [ ] cargo check passes with 0 errors
- [ ] GitExecutor can initialize repo
- [ ] GitExecutor can stage and commit files
- [ ] GitExecutor returns error on failure
</verification>

<success_criteria>
- Generated configs are tracked in git
- Commit message identifies hostname and username
- Previous commit hash is captured for rollback
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution/04-02-SUMMARY.md`
</output>
