---
phase: 04-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/executor/mod.rs
  - src/executor/command.rs
  - src/executor/error.rs
  - src/executor/output.rs
autonomous: true

must_haves:
  truths:
    - "User can execute shell commands with real-time output capture"
    - "Command output streams to TUI as it happens"
    - "Command execution returns success/failure with exit code"
  artifacts:
    - path: "src/executor/mod.rs"
      provides: "Executor module with run_command() function"
      exports: ["run_command", "CommandOutput"]
    - path: "src/executor/command.rs"
      provides: "Command builder for nixos-rebuild and git"
      exports: ["nixos_rebuild_cmd", "git_commit_cmd"]
    - path: "src/executor/error.rs"
      provides: "ExecutorError enum with context"
      exports: ["ExecutorError"]
    - path: "src/executor/output.rs"
      provides: "OutputLine struct for streaming"
      exports: ["OutputLine", "OutputStream"]
---

<objective>
Build execution infrastructure for running shell commands (nixos-rebuild, git) with real-time output capture for TUI display.

Purpose: Enables Phase 4 core capability - running system commands with user-visible progress
Output: Executor module with streaming output capture
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Phase 3 context (what we have)
Phase 3 created generator::generate_all() that writes configs to milos-output/
Phase 3 created UserConfig struct with all user-provided values

# What Phase 4 needs from Phase 3
- generator::generate_all() output path (milos-output/)
- UserConfig with all collected values (hostname, username, etc.)
- 7 generated files: flake.nix, users.nix, git.nix, locale.nix, noctalia.nix, niri/config.kdl, nix.conf
</context>

<tasks>

<task type="auto">
  <name>Create executor module structure</name>
  <files>src/executor/mod.rs, src/executor/error.rs, src/executor/output.rs</files>
  <action>
    Create src/executor/ directory with:
    
    src/executor/mod.rs:
    - Add `pub mod command;`, `pub mod error;`, `pub mod output;`
    - Create `pub fn run_command()` function that:
      - Takes command args as Vec<&str>
      - Returns Result<CommandOutput, ExecutorError>
      - Uses std::process::Command with output() or spawn() for streaming
    
    src/executor/error.rs:
    - Create ExecutorError enum with variants:
      - CommandFailed { command: String, exit_code: Option<i32>, stderr: String }
      - CommandNotFound { command: String }
      - IoError { message: String }
      - OutputCaptureError { message: String }
    - Implement std::fmt::Display for all variants
    - Implement thiserror::Error for ExecutorError
    
    src/executor/output.rs:
    - Create OutputLine struct with fields: line: String, is_stderr: bool, timestamp: DateTime<Utc>
    - Create OutputStream struct as a receiver for output lines
    - Implement Clone, Send for OutputLine
  </action>
  <verify>grep -r "pub fn run_command" src/executor/ && grep -r "ExecutorError" src/executor/</verify>
  <done>Executor module exists with run_command() function and error types</done>
</task>

<task type="auto">
  <name>Implement streaming command execution</name>
  <files>src/executor/command.rs, src/executor/mod.rs</files>
  <action>
    Implement streaming output capture in src/executor/mod.rs:
    - Use std::process::Command::stdout(Stdio::piped()) and stderr(Stdio::piped())
    - Use std::process::Command::spawn() to start process
    - Read stdout/stderr in separate threads using BufReader
    - Yield OutputLine structs via mpsc channel
    - Wait for process completion and get exit code
    - Return CommandOutput with all lines, exit code, duration
    
    Create src/executor/command.rs with builder functions:
    - nixos_rebuild_cmd(flake_path: &PathBuf) -> Command
      - Returns Command configured for "nixos-rebuild switch --flake {path}"
    - git_init_cmd() -> Command
      - Returns Command configured for "git init" in output directory
    - git_commit_cmd(message: &str) -> Command
      - Returns Command configured for "git commit -m {message}"
    - git_add_cmd(path: &str) -> Command
      - Returns Command configured for "git add {path}"
  </action>
  <verify>cargo check 2>&1 | grep -c "^error" && cargo doc --no-deps --document-private-items 2>&1 | head -10</verify>
  <done>Streaming command execution works with output capture</done>
</task>

<task type="auto">
  <name>Add executor module to lib.rs</name>
  <files>src/lib.rs</files>
  <action>
    Add to src/lib.rs:
    - pub mod executor;
    - pub use executor::{run_command, CommandOutput, OutputLine, ExecutorError};
    
    Ensure executor module compiles and exports are accessible
  </action>
  <verify>grep -r "pub mod executor" src/lib.rs && cargo check 2>&1 | grep -c "^error"</verify>
  <done>Executor module is publicly accessible from the library</done>
</task>

</tasks>

<verification>
- [ ] cargo check passes with 0 errors
- [ ] ExecutorError has all required variants
- [ ] run_command() function exists and compiles
- [ ] Command builder functions exist for git and nixos-rebuild
</verification>

<success_criteria>
- Executor module can run any shell command
- Output streams in real-time via OutputLine structs
- Exit codes are captured and returned
- Error cases are handled with descriptive messages
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution/04-01-SUMMARY.md`
</output>
