---
phase: 01-tui-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/main.rs
  - src/lib.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Rust project compiles with Ratatui, Crossterm, and Color-eyre dependencies"
    - "Project has standard Rust project structure (lib.rs, main.rs, modules)"
    - "Color-eyre error handler is installed and working"
  artifacts:
    - path: "Cargo.toml"
      provides: "Rust project with required TUI dependencies"
      contains: "ratatui = \"0.30\""
      contains: "crossterm = \"0.29\""
      contains: "color-eyre = \"0.6\""
    - path: "src/main.rs"
      provides: "Application entry point with color-eyre installation"
      min_lines: 20
    - path: "src/lib.rs"
      provides: "Library root with public exports"
      min_lines: 10
  key_links:
    - from: "src/main.rs"
      to: "src/lib.rs"
      via: "lib = \"milos_niri\" dependency"
      pattern: "lib.*=.*\"milos_niri\""
---

<objective>
Initialize the Rust project structure with all required TUI dependencies.

Purpose: Establish the foundation for the TUI installer by setting up Cargo.toml with Ratatui, Crossterm, and color-eyre, and creating the standard Rust project structure with lib.rs and main.rs.

Output: Compilable Rust project with all dependencies declared and basic project structure.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-tui-foundation/01-RESEARCH.md

# Research findings to apply:
# - Ratatui 0.30.0 + Crossterm 0.29.0 + Color-eyre 0.6 are confirmed compatible
# - Install color-eyre handler early in main() before any errors can occur
# - Use standard Rust project structure (lib.rs for library, main.rs for binary)
</context>

<tasks>

<task type="auto">
  <name>Create Cargo.toml with TUI dependencies</name>
  <files>Cargo.toml</files>
  <action>
    Create Cargo.toml at project root with:
    - Package name: milos_niri
    - Version: 0.1.0
    - Edition: 2024 (or 2021 if 2024 unavailable)
    
    Add these dependencies:
    - ratatui = "0.30.0"
    - crossterm = "0.29.0"
    - color-eyre = "0.6"
    
    Also add for future use (don't remove, they're needed):
    - askama = "0.12" (for templates)
    
    Set crate-type to ["lib", "bin"] for library + binary.
  </action>
  <verify>Run `cargo check` and verify it completes without errors</verify>
  <done>Cargo.toml exists with all required dependencies and project compiles</done>
</task>

<task type="auto">
  <name>Create src/lib.rs with library root</name>
  <files>src/lib.rs</files>
  <action>
    Create src/lib.rs with:
    - Public module declarations (comment placeholders for future modules)
    - Re-export key types that will be used from main.rs
    - Library documentation comment
    
    Keep it minimal but include:
    ```rust
    //! # milos_niri
    //! NixOS TUI Installer - Interactive guided installer for Niri + Noctalia setup
    ```
  </action>
  <verify>Run `cargo check --lib` and verify library compiles</verify>
  <done>src/lib.rs exists and exports are available to binary</done>
</task>

<task type="auto">
  <name>Create src/main.rs with color-eyre installation</name>
  <files>src/main.rs</files>
  <action>
    Create src/main.rs that:
    - Installs color-eyre handler at the very start (CRITICAL - research note)
    - Calls a placeholder run function in lib
    - Handles any errors with color-eyre
    
    Structure:
    ```rust
    use color_eyre::install;
    
    fn main() -> color_eyre::Result<()> {
        // Install color-eyre FIRST - before any errors can occur
        install()?;
        
        // TODO: Call lib function to start app
        println!("Milos Niri - TUI Installer");
        println!("Installer placeholder - more to come in next plans");
        
        Ok(())
    }
    ```
  </action>
  <verify>Run `cargo build --bin milos_niri` and verify binary compiles</verify>
  <done>Binary runs without errors and color-eyre is installed</done>
</task>

</tasks>

<verification>
1. `cargo check` passes for both library and binary
2. Dependencies are resolvable (no conflicts)
3. Binary runs and prints startup message
4. color-eyre handler is installed (can be tested with a panic in dev)
</verification>

<success_criteria>
- Project structure follows Rust conventions (lib.rs + main.rs)
- Cargo.toml declares all TUI dependencies (Ratatui, Crossterm, color-eyre)
- Project compiles without errors
- Color-eyre is installed and ready to catch errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-tui-foundation/01-01-SUMMARY.md`
</output>
