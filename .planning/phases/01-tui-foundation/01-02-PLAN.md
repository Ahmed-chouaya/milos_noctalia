---
phase: 01-tui-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib.rs
  - src/event.rs
  - src/state.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Keyboard input (arrow keys, Tab, Enter, Escape) is captured and processed"
    - "State management store exists with Arc<RwLock<WizardState>> pattern"
    - "Event loop runs at 60fps with non-blocking input polling"
  artifacts:
    - path: "src/event.rs"
      provides: "Event handling with Crossterm polling"
      exports: ["Event", "EventHandler", "run_event_loop"]
      min_lines: 50
    - path: "src/state.rs"
      provides: "WizardState struct with all step data fields"
      exports: ["WizardState", "Step"]
      min_lines: 80
    - path: "src/lib.rs"
      provides: "Module declarations and re-exports"
      updated: true
  key_links:
    - from: "src/event.rs"
      to: "src/state.rs"
      via: "Arc<RwLock<WizardState>> shared state"
      pattern: "Arc.*RwLock.*WizardState"
    - from: "src/main.rs"
      to: "src/event.rs"
      via: "run_event_loop call with state"
---

<objective>
Set up the core framework: Crossterm event loop for keyboard input and centralized state management store.

Purpose: Enable the TUI to respond to user input (arrow keys, Tab, Enter, Escape) and maintain application state across navigation. Uses the Elm Architecture pattern recommended in research.

Output: Working event loop and state store that future plans can build upon.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tui-foundation/01-01-SUMMARY.md
@.planning/phases/01-tui-foundation/01-RESEARCH.md

# Research findings to apply:
# - Use non-blocking `event::poll(Duration)` for input (not blocking read)
# - Elm Architecture: Model = WizardState, Update = Message enum, View = Render
# - State sharing via Arc<RwLock<WizardState>> to avoid clones
# - 16ms tick rate (60fps) for smooth animation timing
</context>

<tasks>

<task type="auto">
  <name>Create src/state.rs with WizardState struct</name>
  <files>src/state.rs</files>
  <action>
    Create src/state.rs with:
    
    1. WizardState struct (flat structure as per context):
       - current_step: usize (current step index, 0-based)
       - completed_steps: Vec<bool> (which steps are done)
       - error_mode: Option<ErrorMode> (for error handling)
       - All step data fields as simple Option<String>:
         - hostname: Option<String>
         - username: Option<String>
         - timezone: Option<String>
         - disk_device: Option<String>
         - etc.
    
    2. Step enum with all wizard steps:
       - Welcome
       - Language (or Locale)
       - Keyboard Layout
       - Network (if needed)
       - Disk Selection
       - User Creation
       - Package Selection
       - Summary/Confirm
    
    3. ErrorMode enum for different error types:
       - InputValidation { field: String, message: String }
       - SystemError { message: String, recoverable: bool }
    
    4. Methods:
       - new() -> WizardState with defaults
       - current_step_mut() -> Option<&mut dyn StepData>
       - mark_step_complete(step: usize)
       - go_to_step(step: usize) -> Result<(), String>
       - can_go_back() -> bool (always true per context)
       - can_go_forward() -> bool (only if current step complete)
    
    Keep structure flat and simple per design decisions.
  </action>
  <verify>Run `cargo check --lib` and verify state module compiles</verify>
  <done>WizardState struct exists with all fields and navigation methods</done>
</task>

<task type="auto">
  <name>Create src/event.rs with Crossterm event handling</name>
  <files>src/event.rs</files>
  <action>
    Create src/event.rs with:
    
    1. Event enum for all input types:
       - Key(KeyEvent) - individual key presses
       - Tick - timer tick for animations
       - Resize(u16, u16) - terminal resize events
    
    2. KeyEvent variants for navigation:
       - Enter (confirm, proceed)
       - Escape (go back, cancel)
       - Tab / Shift+Tab (cycle focus)
       - Up/Down/Left/Right arrows (navigation)
       - Char(char) for text input
       - Ctrl+C for exit
    
    3. EventHandler struct:
       - tick_rate: Duration (16ms for 60fps)
       - user_input_polling_interval: Duration (50ms)
    
    4. run_event_loop function:
       - Takes Arc<RwLock<WizardState>> as parameter
       - Loop with `event::poll(Duration)` for non-blocking input
       - On Key event: update state based on key
       - On Tick event: trigger animation update
       - On Resize: update terminal size for rendering
    
    5. Handle Ctrl+C gracefully (exit with message).
    
    Use crossterm::event::KeyEvent, KeyCode, KeyModifiers.
  </action>
  <verify>Run `cargo check --lib` and verify event module compiles</verify>
  <done>Event loop runs and processes keyboard input without blocking</done>
</task>

<task type="auto">
  <name>Update src/lib.rs with module declarations</name>
  <files>src/lib.rs</files>
  <action>
    Update src/lib.rs to declare and re-export the new modules:
    
    ```rust
    pub mod state;
    pub mod event;
    
    // Re-exports for convenience
    pub use state::{WizardState, Step, ErrorMode};
    pub use event::{Event, EventHandler};
    ```
    
    Ensure modules are declared AFTER the lib doc comment.
  </action>
  <verify>Run `cargo check --lib` and verify all modules integrate</verify>
  <done>src/lib.rs properly exports state and event modules</done>
</task>

</tasks>

<verification>
1. `cargo check --lib` passes
2. WizardState can be created and modified
3. Event loop can be started without errors
4. Keyboard events (Enter, Escape, Tab, arrows) are recognized
5. State is shared via Arc<RwLock<>> without cloning
</verification>

<success_criteria>
- Event loop uses non-blocking Crossterm polling
- State management uses Arc<RwLock<WizardState>> pattern
- Keyboard navigation (arrows, Tab, Enter, Escape) is captured
- Navigation methods (go_to_step, can_go_forward) work correctly
- Error mode can be set and cleared
</success_criteria>

<output>
After completion, create `.planning/phases/01-tui-foundation/01-02-SUMMARY.md`
</output>
