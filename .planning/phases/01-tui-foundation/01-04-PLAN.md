---
phase: 01-tui-foundation
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - src/logo.rs
  - src/wizard.rs
  - Cargo.toml
autonomous: true
user_setup: []

must_haves:
  truths:
    - "MILOS pixel art logo displays on installer startup"
    - "Logo uses neon glow style (green letters + amber glow)"
    - "Typewriter animation shows letters one-by-one with cursor blink"
  artifacts:
    - path: "src/logo.rs"
      provides: "Pixel art logo as Rust code with animation"
      exports: ["Logo, render_logo", "PixelArt"]
      min_lines: 150
    - path: "src/wizard.rs"
      provides: "WelcomeStep uses logo module for rendering"
      updated: true
  key_links:
    - from: "src/logo.rs"
      to: "src/wizard.rs"
      via: "WelcomeStep render calls render_logo"
      pattern: "render_logo"
---

<objective>
Implement the MILOS pixel art logo with neon glow style and typewriter animation effect.

Purpose: Create a visually striking startup screen with the MILOS logo using code-based pixel art (not external files), neon green letters with amber glow, and a typewriter animation with cursor blink as specified in the context.

Output: Animated pixel art logo displayed on WelcomeStep.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tui-foundation/01-03-SUMMARY.md
@.planning/phases/01-tui-foundation/01-CONTEXT.md

# Design decisions to implement:
# - Pixel art defined as Rust code (code-based, not external files)
# - Generator script included for creating/editing logo from simple source format
# - Neon glow style with green letters + amber glow effect
# - Typewriter animation: letters appear one-by-one with cursor blink effect
# - Canvas widget for pixel art with custom paint closure
</context>

<tasks>

<task type="auto">
  <name>Create pixel art logo data structure</name>
  <files>src/logo.rs</files>
  <action>
    Create src/logo.rs with:
    
    1. PixelArt struct or enum for logo representation:
       - Define MILOS letters as 2D array of bool or char
       - Each letter is a grid of pixels (e.g., 5x7 or 8x8)
       - M-I-L-O-S spelling
    
    2. Simple ASCII-based format for easy editing:
       ```rust
       // M
       "█   █ ",
       "█   █ ",
       "█   █ ",
       "█████ ",
       "█   █ ",
       "█   █ ",
       "█   █ ",
       
       // I
       "█████ ",
       "  █   ",
       "  █   ",
       "  █   ",
       "  █   ",
       "  █   ",
       "█████ ",
       ```
    
    3. Include a simple parser/converter function that converts this format to the internal representation.
    
    4. Logo struct with:
       - animation_frame: usize
       - text_progress: usize (how many letters shown)
       - cursor_visible: bool
       - animation_tick: u64
  </action>
  <verify>Run `cargo check --lib` and verify logo module compiles</verify>
  <done>Pixel art data structure exists for MILOS letters</done>
</task>

<task type="auto">
  <name>Implement logo rendering with neon glow style</name>
  <files>src/logo.rs</files>
  <action>
    Implement render_logo function in src/logo.rs:
    
    1. Use Ratatui's Canvas widget with custom paint closure:
       - Create a canvas sized for the logo
       - Paint each "pixel" as a colored block
    
    2. Color scheme (neon glow style):
       - Primary: Green (#00FF00 or similar neon green) for letters
       - Glow: Amber/Orange (#FFB000) for glow effect
       - Background: Dark/Black
    
    3. Glow effect implementation:
       - Draw letter pixels in bright green
       - Draw "halo" pixels in amber around letter edges
       - Or use multiple passes with decreasing intensity
    
    4. Centering: Calculate offset to center logo in available area.
    
    5. Use Ratatui::widgets::Canvas with paint closure:
       ```rust
       Canvas::default()
           .x_bounds(0.0, width as f64)
           .y_bounds(0.0, height as f64)
           .paint(|ctx| {
               for (y, row) in logo_pixels.iter() {
                   for (x, &pixel) in row.iter() {
                       if pixel {
                           ctx.print(x as f64, y as f64, "▀");
                       }
                   }
               }
           })
       ```
  </action>
  <verify>Run `cargo check --lib` and verify rendering compiles</verify>
  <done>Logo renders with neon green letters and amber glow</done>
</task>

<task type="auto">
  <name>Add typewriter animation with cursor blink</name>
  <files>src/logo.rs, src/wizard.rs</files>
  <action>
    Implement typewriter animation:
    
    1. Logo animation state:
       - text_progress: usize (letters shown so far)
       - cursor_blink_timer: u64
       - cursor_visible: bool
       - animation_speed: Duration (e.g., 100ms per letter)
    
    2. Animation update on Tick events:
       - Every animation_speed, increment text_progress
       - Every 500ms, toggle cursor_visible
    
    3. render_logo takes animation state and renders:
       - Only letters up to text_progress
       - Blinking cursor after last shown letter
       - Use block element "█" for cursor
    
    4. WelcomeStep uses render_logo:
       - On Tick: update animation state
       - On render: call render_logo with current state
       - When text_progress == full, show "Press Enter to begin" below
    
    Update Cargo.toml if additional dependencies needed for timing.
    
    Per context: "Exact pixel art design for the MILOS logo" is Claude's discretion.
  </action>
  <verify>Run `cargo build --bin milos_niri` and verify animation compiles</verify>
  <done>Logo animates with typewriter effect and blinking cursor</done>
</task>

</tasks>

<verification>
1. Binary runs and displays animated logo on WelcomeStep
2. Letters appear one-by-one with typewriter effect
3. Cursor blinks after each letter (and after full logo)
4. Logo uses neon green with amber glow styling
5. Logo is centered on screen
6. After animation completes, "Press Enter to begin" prompt appears
7. Pressing Enter proceeds to next step
</verification>

<success_criteria>
- MILOS pixel art logo displays on installer startup
- Neon glow style with green letters + amber glow effect
- Typewriter animation with cursor blink works smoothly
- Logo animation completes before "Press Enter to begin" appears
- Design matches Noctalia theme (green/amber glow)
</success_criteria>

<output>
After completion, create `.planning/phases/01-tui-foundation/01-04-SUMMARY.md`
</output>
