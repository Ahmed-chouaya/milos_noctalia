---
phase: 01-tui-foundation
plan: "05"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/error.rs
  - src/state.rs
  - src/wizard.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Input validation errors show inline message below field with auto-focus"
    - "System errors (disk, network) display as modal overlay blocking navigation"
    - "Error modal shows user-friendly text by default with toggle to show color-eyre backtrace"
    - "Modal actions are context-dependent (Retry, Cancel, Exit)"
  artifacts:
    - path: "src/error.rs"
      provides: "Error handling with modal overlay and backtrace toggle"
      exports: ["ErrorModal", "show_error", "ErrorType"]
      min_lines: 120
    - path: "src/state.rs"
      provides: "ErrorMode enum and error state in WizardState"
      updated: true
  key_links:
    - from: "src/error.rs"
      to: "src/wizard.rs"
      via: "Modal rendering blocks navigation during errors"
      pattern: "ErrorModal.*render"
    - from: "src/wizard.rs"
      to: "src/state.rs"
      via: "state.error_mode for tracking active errors"
      pattern: "error_mode.*Some"
---

<objective>
Implement beautiful error handling with color-eyre and user-friendly error modals.

Purpose: Display errors beautifully with inline validation messages for input errors and modal overlays for system errors. Include a toggle to show color-eyre backtraces for debugging, with context-dependent modal actions.

Output: Complete error handling system with visual error display.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tui-foundation/01-02-SUMMARY.md
@.planning/phases/01-tui-foundation/01-CONTEXT.md
@.planning/phases/01-tui-foundation/01-RESEARCH.md

# Design decisions to implement:
# - Input validation errors: inline message below field, auto-focuses field, shows suggestion
# - System errors (disk, network): modal overlay blocking navigation until dismissed
# - Modal actions: context-dependent (Retry, Cancel, Exit - varies by error type)
# - Error message style: user-friendly text by default, toggle to show color-eyre backtrace
# - Critical: Always restore terminal before displaying color-eyre errors
</context>

<tasks>

<task type="auto">
  <name>Define ErrorType enum and error structures</name>
  <files>src/error.rs</files>
  <action>
    Create src/error.rs with:
    
    1. ErrorType enum:
       - InputValidation { field: String, message: String, suggestion: Option<String> }
       - DiskError { message: String, device: Option<String> }
       - NetworkError { message: String, operation: String }
       - ConfigError { message: String, setting: String }
       - Other { message: String }
    
    2. ErrorModal struct:
       - error_type: ErrorType
       - backtrace_visible: bool (toggle state)
       - selected_action: ErrorAction (for button selection)
    
    3. ErrorAction enum (context-dependent):
       - Retry (for recoverable errors)
       - Cancel (for optional operations)
       - Exit (for fatal errors)
       - Dismiss (for non-blocking warnings)
    
    4. Methods:
       - from_error(error: and impl std::error::Error) -> ErrorType
       - action_buttons(and self) -> Vec<&'static str>
       - is_fatal(and self) -> bool
  </action>
  <verify>Run `cargo check --lib` and verify error module compiles</verify>
  <done>ErrorType and ErrorModal structures exist</done>
</task>

<task type="auto">
  <name>Update WizardState for error mode handling</name>
  <files>src/state.rs</files>
  <action>
    Update src/state.rs:
    
    1. Update WizardState:
       - Add error_mode: Option<ErrorModal>
       - Add focused_field: Option<String> (for input validation focus)
    
    2. Methods for error handling:
       - set_error(error: ErrorType) -> sets error_mode
       - clear_error() -> clears error_mode
       - set_focus(field: andstr) -> sets focused_field
       - has_error() -> bool
       - current_error() -> Option<&ErrorModal>
    
    3. For input validation:
       - validate_field(field: andstr, value: andstr, validator: fn(ands) -> Result<(), String>)
       - On error: set_error with InputValidation type, set focused_field
    
    Ensure ErrorModal is imported from error module.
  </action>
  <verify>Run `cargo check --lib` and verify state module compiles</verify>
  <done>WizardState tracks error mode and focused fields</done>
</task>

<task type="auto">
  <name>Implement error modal rendering and navigation</name>
  <files>src/error.rs, src/wizard.rs</files>
  <action>
    Implement error modal rendering:
    
    1. render_error_modal function in src/error.rs:
       - Takes frame, modal, and area
       - Uses Block, Borders, Title
       - Displays user-friendly message prominently
       - Shows "Toggle backtrace" button/link
       - Shows action buttons (Retry/Cancel/Exit) based on error type
       - Handles backtrace display using color-eyre
    
    2. In src/wizard.rs, update run_wizard loop:
       - Check state.error_mode before rendering normal UI
       - If error_mode.is_some():
         - Render semi-transparent overlay
         - Render error modal centered
         - Block normal navigation (except error actions)
         - Handle Tab to cycle between action buttons
         - Handle Enter to select action
         - Handle 't' or 'd' to toggle backtrace visible
    
    3. Action handling:
       - Retry: clear_error(), retry operation
       - Cancel: clear_error(), stay on current step
       - Exit: restore terminal, exit application
       - Dismiss: clear_error(), continue
    
    4. For input validation errors (inline):
       - Render message below the field with error style (red)
       - focused_field causes automatic focus on that field
       - Show suggestion if available
    
    Per context: "Error modal button ordering and labels" is Claude's discretion.
  </action>
  <verify>Run `cargo build --bin milos_niri` and verify error handling compiles</verify>
  <done>Error modal renders with user-friendly message and backtrace toggle</done>
</task>

</tasks>

<verification>
1. Input validation errors show inline below field with red styling
2. System errors show modal overlay that blocks navigation
3. Modal shows user-friendly error text
4. Toggle ('t' key) shows/hides color-eyre backtrace
5. Action buttons (Retry/Cancel/Exit) work correctly
6. Tab cycles between buttons, Enter selects
7. color-eyre backtraces are properly formatted
8. Terminal is restored before displaying color-eyre panics
</verification>

<success_criteria>
- Input validation errors display inline with auto-focus
- System errors display as modal overlay blocking navigation
- Error modal shows context-dependent actions (Retry, Cancel, Exit)
- User-friendly text shown by default, toggle shows color-eyre backtrace
- Errors are clear, actionable, and beautiful
</success_criteria>

<output>
After completion, create `.planning/phases/01-tui-foundation/01-05-SUMMARY.md`
</output>
