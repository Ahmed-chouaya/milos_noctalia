---
phase: 03-configuration-generation
plan: "03-03"
type: execute
wave: 1
depends_on: ["03-01", "03-02"]
files_modified: ["src/generator/write.rs", "src/generator/validate.rs"]
autonomous: true

must_haves:
  truths:
    - "Template substitution validated before writing"
    - "Files written atomically with temp-then-rename"
    - "Parent directories created automatically"
  artifacts:
    - path: "src/generator/write.rs"
      provides: "Atomic file writing with directory creation"
      contains: "AtomicFile"
    - path: "src/generator/validate.rs"
      provides: "Template substitution validation"
      contains: "validate_no_unsubstituted"
  key_links:
    - from: "src/generator/flake.rs"
      to: "src/generator/write.rs"
      via: "write_config_atomically() called after generation"
      pattern: "write_config_atomically"
    - from: "src/generator/validate.rs"
      to: "src/generator/templates/flake.nix"
      via: "checks for {{ and }} patterns"
      pattern: "\\{\\{.*\\}\\}"
---

<objective>
Implement validation and atomic file writing utilities for generated configurations.

Purpose: Validates that all template variables were substituted (no {{ placeholder }} syntax remaining) and writes files atomically to prevent corruption. This ensures generated configs are complete and safe to write.
Output: validate_template_substitution() and write_config_atomically() functions.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-configuration-generation/03-RESEARCH.md
@.planning/phases/03-configuration-generation/03-01-PLAN.md
@.planning/phases/03-configuration-generation/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement template substitution validation</name>
  <files>src/generator/validate.rs</files>
  <action>
    Create src/generator/validate.rs with validation functions.

    Function: validate_no_unsubstituted(content: &str) -> Result<(), GeneratorError>
    - Check for {{ and }} patterns that indicate unsubstituted variables
    - Return error with template name if patterns found
    - Handle edge cases: escaped braces {{ }} in content should not trigger false positives

    Function: validate_nix_syntax(content: &str) -> Result<(), GeneratorError>
    - Basic Nix syntax checks (balanced braces, quotes)
    - This is lightweight - full Nix evaluation happens during nixos-rebuild

    Export these functions from src/generator/mod.rs.
  </action>
  <verify>validate_no_unsubstituted detects {{ placeholder }} in test content</verify>
  <done>Template validation prevents unsubstituted placeholders from being written</done>
</task>

<task type="auto">
  <name>Task 2: Implement atomic file writing</name>
  <files>src/generator/write.rs</files>
  <action>
    Create src/generator/write.rs with atomic writing functions.

    Function: write_config_atomically(path: &Path, content: &str) -> Result<(), GeneratorError>
    - Use atomicwrites crate with OverwriteBehavior::AllowOverwrite
    - Ensure parent directories exist before writing (create_dir_all)
    - Return GeneratorError::FileWrite on failure with path context

    Function: write_config(path: &Path, content: &str) -> Result<(), GeneratorError>
    - Wrapper that calls validate_no_unsubstituted first
    - Then calls write_config_atomically
    - Returns early with validation error if substitution incomplete

    Export these functions from src/generator/mod.rs.
  </action>
  <verify>write_config_atomically creates parent directories and writes file atomically</verify>
  <done>Atomic file writing prevents corruption on system crash during write</done>
</task>

<task type="auto">
  <name>Task 3: Add validation to all generators</name>
  <files>src/generator/flake.rs, src/generator/users.rs, src/generator/git.rs, src/generator/locale.rs, src/generator/noctalia.rs, src/generator/niri.rs, src/generator/nixconf.rs</files>
  <action>
    Update each Generator implementation to include validation:

    Add to Generator trait (in mod.rs):
    ```rust
    fn validate(&self, content: &str) -> Result<(), ValidationError> {
        validate_no_unsubstituted(content)
    }
    ```

    Or implement inline in each generator:
    ```rust
    fn validate(&self, content: &str) -> Result<(), GeneratorError> {
        validate_no_unsubstituted(content)
    }
    ```

    This ensures each generator validates its output before write_config calls it.
  </action>
  <verify>Each generator's validate() method is called before writing</verify>
  <done>All 7 generators validate output before file write</done>
</task>

</tasks>

<verification>
- validate_no_unsubstituted correctly identifies unsubstituted templates
- write_config_atomically creates directories and writes files atomically
- write_config fails fast when templates have unsubstituted variables
- All generators call validate() before writing
</verification>

<success_criteria>
- [ ] validate_no_unsubstituted() detects {{ patterns in generated content
- [ ] write_config_atomically() writes files using atomicwrites crate
- [ ] Parent directories created automatically before writing
- [ ] Validation errors prevent file writing with helpful messages
- [ ] All generators call validate() as part of generation pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/03-configuration-generation/03-03-SUMMARY.md`
</output>
