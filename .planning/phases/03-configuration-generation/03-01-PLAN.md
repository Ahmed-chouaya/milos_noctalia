---
phase: 03-configuration-generation
plan: "03-01"
type: execute
wave: 1
depends_on: []
files_modified: ["Cargo.toml", "src/lib.rs", "src/generator/mod.rs", "src/generator/error.rs", "src/generator/context.rs"]
autonomous: true

must_haves:
  truths:
    - "Dependencies added to Cargo.toml"
    - "Generator module structure created"
    - "UserConfig struct matches Phase 2 state fields"
    - "GeneratorError enum handles all error cases"
  artifacts:
    - path: "src/generator/mod.rs"
      provides: "Generator trait and module organization"
      contains: "pub trait Generator"
    - path: "src/generator/error.rs"
      provides: "Error types for generation failures"
      contains: "pub enum GeneratorError"
    - path: "src/generator/context.rs"
      provides: "UserConfig struct from Phase 2 state"
      contains: "struct UserConfig"
  key_links:
    - from: "src/generator/context.rs"
      to: "src/state.rs"
      via: "UserConfig fields match WizardState fields"
      pattern: "hostname.*username.*timezone"
---

<objective>
Set up the generator module infrastructure with dependencies, error types, and shared context structures.

Purpose: Establishes the foundation for all configuration generation - without this, templates cannot be rendered or written. This plan adds askama, atomicwrites, thiserror, and anyhow to Cargo.toml, then creates the Generator trait, error enum, and UserConfig struct that all generators will use.
Output: Working generator module with dependencies, error handling, and context structures.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-configuration-generation/03-RESEARCH.md
@src/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generator dependencies to Cargo.toml</name>
  <files>Cargo.toml</files>
  <action>
    Add the following dependencies to Cargo.toml:
    ```
    askama = "0.15"
    atomicwrites = "0.4"
    thiserror = "1.0"
    anyhow = "1.0"
    ```

    Use `cargo add` to ensure proper version handling:
    ```bash
    cargo add askama@=0.15 atomicwrites@=0.4 thiserror anyhow
    ```

    Remove any existing askama version if present (should be 0.12 from RESEARCH.md).
  </action>
  <verify>cargo check passes without errors about missing dependencies</verify>
  <done>Cargo.toml contains askama 0.15, atomicwrites 0.4, thiserror 1.0, and anyhow 1.0</done>
</task>

<task type="auto">
  <name>Task 2: Create generator module structure</name>
  <files>src/generator/mod.rs</files>
  <action>
    Create src/generator/mod.rs with:
    1. Module declarations (flake.rs, users.rs, git.rs, locale.rs, noctalia.rs, niri.rs, nixconf.rs as stubs)
    2. Generator trait definition following research pattern:
       ```rust
       pub trait Generator {
           fn generate(&self, config: &UserConfig) -> Result<GeneratedFile>;
           fn output_path(&self, config: &UserConfig) -> PathBuf;
       }
       ```
    3. GeneratedFile struct with path and content fields
    4. Re-export all generator modules

    Create empty stub files for all generator modules to allow compilation:
    - src/generator/flake.rs (empty mod)
    - src/generator/users.rs (empty mod)
    - src/generator/git.rs (empty mod)
    - src/generator/locale.rs (empty mod)
    - src/generator/noctalia.rs (empty mod)
    - src/generator/niri.rs (empty mod)
    - src/generator/nixconf.rs (empty mod)
  </action>
  <verify>cargo check passes, src/generator/ directory exists with all module files</verify>
  <done>src/generator/mod.rs exists with Generator trait and all submodule stubs</done>
</task>

<task type="auto">
  <name>Task 3: Define GeneratorError enum</name>
  <files>src/generator/error.rs</files>
  <action>
    Create src/generator/error.rs with GeneratorError enum using thiserror.

    Include all variants from research pattern:
    - TemplateError (from askama::Error)
    - ValueMissing (field name)
    - Validation (message)
    - FileWrite (path, source)
    - UnsubstitutedPlaceholder (template name)

    Implement From<askama::Error> for GeneratorError with wildcard match for non-exhaustive enum.

    Add #[derive(Debug)] and #[error] macros.
  </action>
  <verify>cargo check passes, GeneratorError enum is importable from generator::error</verify>
  <done>GeneratorError enum handles template, validation, and file write errors with user-friendly messages</done>
</task>

<task type="auto">
  <name>Task 4: Create UserConfig struct from Phase 2 state</name>
  <files>src/generator/context.rs</files>
  <action>
    Create src/generator/context.rs with UserConfig struct.

    Fields MUST match WizardState from src/state.rs:
    ```rust
    pub struct UserConfig {
        pub hostname: String,
        pub username: String,
        pub full_name: String,
        pub git_username: String,
        pub git_email: String,
        pub timezone: String,
        pub keyboard_layout: String,
        pub wallpaper_dir: String,
        pub avatar_path: Option<String>,
        pub screenshot_dir: String,
    }
    ```

    Add a constructor that converts from WizardState (takes &WizardState, extracts and clones Option fields as Strings).
  </action>
  <verify>cargo check passes, UserConfig can be constructed from WizardState reference</verify>
  <done>UserConfig struct has all 10 fields from Phase 2 with proper types (Strings, not Options)</done>
</task>

</tasks>

<verification>
- cargo check passes with no warnings
- All generator module files exist and compile
- UserConfig can be created from WizardState
- GeneratorError implements From<askama::Error>
</verification>

<success_criteria>
- [ ] Dependencies added: askama 0.15, atomicwrites 0.4, thiserror 1.0, anyhow 1.0
- [ ] src/generator/mod.rs exists with Generator trait and module organization
- [ ] src/generator/error.rs exists with GeneratorError enum
- [ ] src/generator/context.rs exists with UserConfig struct and From<WizardState> impl
- [ ] All submodule stubs exist (flake.rs, users.rs, git.rs, locale.rs, noctalia.rs, niri.rs, nixconf.rs)
</success_criteria>

<output>
After completion, create `.planning/phases/03-configuration-generation/03-01-SUMMARY.md`
</output>
