---
phase: 03-configuration-generation
plan: "03-06"
type: execute
wave: 3
depends_on: ["03-05"]
files_modified: ["milos-output/.gitkeep"]
autonomous: false

must_haves:
  truths:
    - "Complete wizard flow works end-to-end"
    - "Generated configs have all user values substituted"
    - "Generated Nix files are syntactically valid"
  artifacts:
    - path: "milos-output/flake.nix"
      contains: "hostname and username substituted"
    - path: "milos-output/modules/users.nix"
      contains: "username and groups"
    - path: "milos-output/modules/git.nix"
      contains: "git_username and git_email"
    - path: "milos-output/modules/locale.nix"
      contains: "timezone and keyboard_layout"
    - path: "milos-output/modules/noctalia.nix"
      contains: "wallpaper_dir"
    - path: "milos-output/modules/niri/config.kdl"
      contains: "screenshot_dir"
    - path: "milos-output/nix.conf"
      contains: "trusted-users with username"
---

<objective>
Verify the complete configuration generation flow with a human verification checkpoint.

Purpose: Human verifies that:
1. All placeholders are substituted (no {{ }} syntax in output)
2. Generated files have correct values from wizard input
3. Nix syntax is valid (can be parsed)
4. File structure is correct (modules in subdirectory)

This is a checkpoint:human-verify task that pauses for human confirmation.
Output: Verified generated configuration files.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-configuration-generation/03-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the complete wizard and generate configs</name>
  <files>milos-output/</files>
  <action>
    Run the TUI with test data to generate configurations:

    1. Build the project:
       ```bash
       cargo build --release
       ```

    2. Run the installer and complete wizard with test data:
       - Welcome: Enter
       - Timezone: Type "New_York", select America/New_York
       - Keyboard: Type "us", select US (QWERTY)
       - Account:
         - Hostname: "testhost"
         - Username: "testuser"
         - Full Name: "Test User"
         - Git Username: "testuser"
         - Git Email: "test@example.com"
       - Paths: Use defaults
       - Summary: Enter to proceed
       - Generate: Enter to generate

    3. Check generated files:
       ```bash
       ls -la milos-output/
       ls -la milos-output/modules/
       cat milos-output/flake.nix
       ```
  </action>
  <verify>cargo build succeeds, milos-output directory contains generated files</verify>
  <done>Generated files exist in milos-output/ and milos-output/modules/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 3 configuration generation system:
    - 7 Askama templates (flake.nix, users.nix, git.nix, locale.nix, noctalia.nix, niri/config.kdl, nix.conf)
    - Generator trait and implementations for all 7 configs
    - Validation (no unsubstituted placeholders)
    - Atomic file writing
    - GenerateStep in TUI wizard flow

    Generated files should be in milos-output/ directory.
  </what-built>
  <how-to-verify>
    1. Check milos-output/flake.nix contains testhost (hostname) and testuser (username), no {{ }} patterns
    2. Check milos-output/modules/users.nix contains testuser with groups: wheel, sudo, audio, video, users
    3. Check milos-output/modules/git.nix contains test@example.com and testuser
    4. Check milos-output/modules/locale.nix contains America/New_York and us keyboard layout
    5. Check milos-output/modules/noctalia.nix contains ~/Pictures/Wallpapers path
    6. Check milos-output/modules/niri/config.kdl contains ~/Pictures/Screenshots path
    7. Check milos-output/nix.conf contains trusted-users = root testuser
    8. Verify no {{ placeholder }} syntax appears in any file
    9. Optionally validate Nix syntax: `nix-instantiate --parse milos-output/flake.nix`
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 files look correct with properly substituted values and no {{ placeholders }}.
    
    Or describe any issues found:
    - Missing substitutions
    - Template syntax remaining
    - Wrong values
    - Missing files
  </resume-signal>
</task>

</tasks>

<verification>
- cargo build succeeds
- 7 files generated in correct locations
- All user values substituted correctly
- No template syntax ({{ }}) remaining
- Nix syntax is valid
</verification>

<success_criteria>
- [ ] cargo build --release succeeds
- [ ] 7 configuration files generated in milos-output/ and milos-output/modules/
- [ ] All placeholders substituted with test values
- [ ] No {{ template syntax }} in output
- [ ] Human verifies all 7 files are correct
</success_criteria>

<output>
After completion, create `.planning/phases/03-configuration-generation/03-06-SUMMARY.md`
</output>
