---
phase: 02-input-collection
plan: "02-03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state.rs
  - src/wizard.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can configure wallpaper directory with pre-filled default"
    - "User can configure optional avatar image path"
    - "User can configure screenshot directory with pre-filled default"
    - "Path inputs support basic editing (typing, backspace)"
    - "Optional avatar field can be left empty"
  artifacts:
    - path: "src/state.rs"
      contains: "wallpaper_dir, avatar_path, screenshot_dir fields"
    - path: "src/wizard.rs"
      contains: "PathsStep struct for path configuration"
      implements: "WizardStep with 3 path fields"
    - path: "src/wizard.rs"
      contains: "render_paths_form() with path inputs"
  key_links:
    - from: "PathsStep"
      to: "WizardState"
      via: "state.wallpaper_dir, state.avatar_path, state.screenshot_dir"
      pattern: "state\\.(wallpaper_dir|avatar_path|screenshot_dir)"
---

<objective>
Implement path configuration step with wallpaper, avatar, and screenshot paths with sensible defaults.

**Purpose:** Users need to configure where Noctalia looks for wallpapers, where to find their avatar, and where to save screenshots. Defaults are pre-filled but editable.

**Output:**
- PathsStep wizard step with 3 path fields
- Default values: ~/Pictures/Wallpapers, ~/Pictures/Screenshots, avatar optional
- Path editing with basic keyboard input
- Optional avatar field (can be empty/none)
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-input-collection/02-CONTEXT.md
@src/state.rs
@src/wizard.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add path fields to WizardState</name>
  <files>src/state.rs</files>
  <action>
    Add path fields to WizardState struct:

    ```rust
    pub wallpaper_dir: Option<String>,
    pub avatar_path: Option<String>,  // Optional - can be None
    pub screenshot_dir: Option<String>,
    ```

    In WizardState::new(), set defaults:
    - wallpaper_dir: Some("~/Pictures/Wallpapers".to_string())
    - avatar_path: None  // Optional, user can leave empty
    - screenshot_dir: Some("~/Pictures/Screenshots".to_string())

    Add validation method:
    - `validate_path()`: Checks path is not empty, optional sanity check for tilde expansion
    - Avatar path validation is optional (can be None)
  </action>
  <verify>`cargo check --lib 2>&1 | grep -c "error"` - 0 errors</verify>
  <done>WizardState has path configuration fields with defaults</done>
</task>

<task type="auto">
  <name>Task 2: Create PathsStep struct for path configuration</name>
  <files>src/wizard.rs</files>
  <action>
    Create PathsStep struct:

    ```rust
    pub struct PathsStep {
        focus_field: usize,  // 0: wallpaper, 1: avatar, 2: screenshot
        wallpaper_buffer: String,
        avatar_buffer: String,
        screenshot_buffer: String,
    }
    ```

    Field order:
    0. Wallpaper directory (required)
    1. Avatar path (optional)
    2. Screenshot directory (required)

    Implement new() that initializes buffers from WizardState defaults (or empty strings for fresh start).

    Implement WizardStep trait:
    - `title()`: Returns "Paths & Directories"
    - `is_complete()`: Returns wallpaper_dir.is_some() && screenshot_dir.is_some()
  </action>
  <verify>`cargo check --lib 2>&1 | grep -c "error"` - 0 errors for PathsStep</verify>
  <done>PathsStep struct exists with 3 path field focus management</done>
</task>

<task type="auto">
  <name>Task 3: Implement PathsStep render with path inputs</name>
  <files>src/wizard.rs</files>
  <action>
    Implement render() for PathsStep:

    Render 3 fields vertically:
    - Field label with focus indicator: "▶ " or "  "
    - Path value with placeholder if empty
    - For optional avatar field, show "(optional)" label

    Layout:
    ```
      ▶ Wallpaper Directory: ~/Pictures/Wallpapers
        Avatar Image: ~/avatar.png (optional)
        Screenshot Directory: ~/Pictures/Screenshots

      These paths are used by Noctalia for:
      • Finding wallpaper images
      • Displaying your avatar in UI
      • Saving screenshots
    ```

    Visual distinction: Use slightly different styling for optional vs required fields.

    Per 02-CONTEXT.md: "Path inputs complete - User configures wallpaper directory, optional avatar path, and screenshot path with defaults pre-filled"
  </action>
  <verify>`cargo check --lib 2>&1 | grep -c "error"` - 0 errors for render</verify>
  <done>PathsStep renders all 3 path fields with defaults and labels</done>
</task>

<task type="auto">
  <name>Task 4: Implement PathsStep input handling</name>
  <files>src/wizard.rs</files>
  <action>
    Implement handle_input() for PathsStep:

    Key handling (similar to AccountStep):
    - **Up/Down/Tab/BackTab**: Navigate between fields (0 → 1 → 2 → 0)
    - **Enter**: 
      - Save current field to buffer
      - If required field (0 or 2) is empty, show error
      - If required field has content, advance to next
      - On last field (2) with valid data, mark complete and go_next()
    - **Char(c)**: Append to current field buffer
    - **Backspace**: Remove from current field buffer
    - **Ctrl+U**: Clear current field buffer

    Avatar field (field 1) is optional:
    - Can be left empty (None in state)
    - Enter on empty avatar field advances without error
  </action>
  <verify>`cargo check --lib 2>&1 | grep -c "error"` - 0 errors for input handling</verify>
  <done>PathsStep handles path editing and navigation</done>
</task>

<task type="auto">
  <name>Task 5: Add PathsStep to wizard flow and validate</name>
  <files>src/wizard.rs, src/state.rs</files>
  <action>
    Add PathsStep between Account and Summary:

    First, add Step::Paths to Step enum before Summary:

    ```rust
    pub enum Step {
        Welcome,
        Timezone,
        Keyboard,
        Account,
        Paths,  // NEW
        Summary,
    }
    ```

    Update Step::all_steps() to include Paths.

    Update create_current_step():
    ```rust
    Step::Paths => Box::new(PathsStep::new()),
    ```

    Update WizardState::new() for new step count.

    Update SummaryStep to display path values.

    Per 02-CONTEXT.md: "Path inputs complete - User configures wallpaper directory, optional avatar path, and screenshot path with defaults pre-filled"
  </action>
  <verify>`cargo build 2>&1 | tail -5` - builds successfully</verify>
  <done>PathsStep integrated as step between Account and Summary</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. `cargo build` succeeds with no warnings
2. All 3 path fields are storable in WizardState
3. Defaults appear pre-filled on first visit
4. Required fields (wallpaper, screenshot) cannot be left empty
5. Optional avatar field can be empty
6. Tab/arrow navigation works between fields
7. Summary step shows configured paths
</verification>

<success_criteria>
- [ ] Wallpaper directory with default (INP-08)
- [ ] Avatar image path optional (INP-09)
- [ ] Screenshot directory with default (INP-10)
- [ ] Path editing with basic keyboard input
- [ ] Validation that required paths are not empty
- [ ] Paths displayed in Summary step
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-collection/02-03-SUMMARY.md`
</output>
