{ config, pkgs, ... }:

{
  programs.alacritty = {
    enable = true;

    # Settings (to be populated in Task 31)
    settings = {
      # TODO: Extract from ~/.config/alacritty/alacritty.toml
    };
  };
}
